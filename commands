
python3 -m venv .venv (creer env virtuel) 
echo .venv >> .gitignore 
source .venv/bin/activate (activer env virtuel) 
pip install -U pip 


(moyen de communication entre github et local)
ssh-keygen -t 
cat ~/.ssh/ 
